# GLO-4035

## Build Setup

* NPM

``` bash
# install dependencies
npm install

# serve at localhost:3000
npm dev
```
* YARN

``` bash
# install dependencies
yarn

# serve at localhost:3000
yarn dev
```